<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fastretailing.dcp.storecommon.report.api.repository.GeneralStoreTimeZoneMapper">

    <select id="selectGeneralItem" resultType="String">
        SELECT /* GeneralStoreTimeZoneMapper_001 */
            ${targetField}
        FROM
            ${tableName} 
        WHERE
            ${conditionField}=#{generalPurposeType}
        AND store_code=#{storeCode}
    </select>
</mapper>