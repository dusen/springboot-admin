####################################################################################################
# Current platform's information.
####################################################################################################
platform:
  name:
    short: SLS
    full: sales
  version: v1

####################################################################################################
# Context path of the application.
####################################################################################################
server:
  context-path: /${platform.name.full}/${platform.version}
  port: 8081
  session:
    timeout: 60
    
spring:
  messages:
    basename: messages,messages-import-transaction,messages-dcp-common
    cache-seconds: -1
    encoding: UTF-8
  
  session:
    store-type: jdbc
    
  datasource:
    url: jdbc:postgresql://unittest-db.ap-northeast-1.maintenance:5432/unittest
    username: testuser
    password: DxxuireMqm6b
    driverClassName: org.postgresql.Driver
    
  #datasource:
    #url: jdbc:postgresql://172.31.11.63:5432/TecDB
    #username: nec
    #password: nec
    #driverClassName: org.postgresql.Driver
   
  redis:
    host: RedisServer
    port: 6379
    pool:
      max-active: 100
      max-idle: 100
      min-idle: 0
      max-wait: -1

  transaction:
    default-timeout: 60

cloud:
  aws:
    stack:
      auto: false

logging:
  level:
    com.fastretailing.dcp: DEBUG
#    root: DEBUG
#    org.springframework.web: DEBUG
  file: 
  path: logs
  
version: 
  aws: 1.11.228

transaction-dispatch:
  sqs: 
    queue-name: DynamoTransactionQueue
    connection-timeout: 10000
    request-timeout: 0
    backoff-strategy: default
    max-error-retry: 3
  dynamodb:
    transaction-status-table: ${dynamo_table_name_status}
    region: ap-northeast-1
    connection-timeout: 10000
    request-timeout: 0
    backoff-strategy: default
    max-error-retry: 10
  aws-region: ap-northeast-1
  channel-code: NORMAL
  max-number-of-message: 10
  with-wait-time-seconds: 20
  businessReceiverName: transaction-import
  awsRegion: UTC
